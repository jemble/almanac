p There are {{events.length}} events
ul
    li(ng-repeat="event in events | orderBy:'startDate'")
        h3 {{event.startDate | date:'dd MMMM yyyy'}} - {{event.location}}
        a(href='editEvent/{{event.id}}') More
